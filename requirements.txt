# FastMCP - MCP server framework
fastmcp>=0.4.0

# FastAPI and web framework dependencies (explicitly included in case fastmcp doesn't bundle them)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0

# AWS SDK for Python
boto3>=1.34.0

# Configuration management
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Environment variables
python-dotenv>=1.0.0

# Structured logging
structlog>=24.1.0
